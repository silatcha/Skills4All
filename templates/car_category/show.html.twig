{% extends 'base.html.twig' %}

{% block title %}CarCategory{% endblock %}

{% block body %}
    <h1>CarCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_category.id }}</td>
            </tr>
            <tr>
                <th>NameCategory</th>
                <td>{{ car_category.nameCategory }}</td>
            </tr>
        </tbody>
    </table>
<div class="row">
<div class="col-md-1">
    <a href="{{ path('app_car_category_edit', {'id': car_category.id}) }}"class="btn btn-secondary">edit</a>
</div>
<div class="col-md-1">
    {{ include('car_category/_delete_form.html.twig') }}
</div>

</div>
    
{% endblock %}
